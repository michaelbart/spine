<!--
docs/decisions/INDEX.md — never hand-written from this template directly.
Generated exclusively by `core/scripts/decision-index --project <path>`,
which produces the header and table below mechanically (grep/awk over
each D-*.md record's fixed field shape — no LLM call, no summarization).
This file exists only as a triage aid so a researcher (or a human) can
see every decision's id/title/category/status/scope/supersession chain
at a glance before deciding which 2-3 records are worth opening — it is
never itself a citation target. Every real claim still opens the actual
`D-<n>-*.md` record and computes its own `core/scripts/decision-hash`.

Regenerated at the same two points a docs/decisions/ record is ever
written — /design (§4, before its final commit) and /ship (§2, after a
task implements or distills a decision) — never as a separate manual
step, matching docs/map.md's own "never hand-maintained" discipline.
-->

# Decision index

Generated by `core/scripts/decision-index`. Never hand-maintained — if a
row here is wrong, regenerate rather than editing this file directly. A
triage aid only: every real citation still opens the actual `D-<n>-*.md`
record and computes its own `core/scripts/decision-hash` — nothing here
is cited directly.

| Id | Title | Category | Status | Scope | Supersedes | Superseded-by |
|---|---|---|---|---|---|---|
