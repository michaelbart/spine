<!-- spine:map
sha: <full commit sha this map was generated against>
generated: <yyyy-mm-ddThh:mm:ssZ>
-->

# Architecture map

Generated by `/remap`. Never hand-maintained — if something here is wrong,
fix it by running `/remap` again, not by editing this file directly. On a
new project this starts near-empty; that's correct. A rich map with a stale
stamp is the failure mode, not an empty map with a fresh one.

The research skill reads the `sha:` stamp above and diffs it against the
current tree the same way `check-stale` does for research grounding files
(see `core/scripts/check-stale`); when this map has drifted materially since
generation, research notes that explicitly rather than trusting it silently.

## Module boundaries

<!-- The real seams: what owns what, and what's expected to reach across a
     boundary vs. what would be a layering violation if it did. -->

## Data flow

<!-- How data actually moves for the 2-3 flows that matter most: entry to
     persistence, and back out. Not a diagram of everything — the paths a
     researcher would need to trace to understand a change's blast radius. -->

## Entry points

<!-- Where execution starts: routes, handlers, CLI commands, scheduled jobs,
     whatever this stack's equivalent is. -->

## Known weirdness

<!-- Half-finished migrations, undocumented invariants, dead code nobody's
     removed, naming that doesn't match reality. Surfaced, not fixed here —
     this is where /adopt's initial survey (and every /remap since) writes
     down what it found without pretending it isn't there. -->
